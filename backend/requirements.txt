# Backend (Flask) dependencies.
# Versions below are the ones the app has been verified to run with.
# NOTE: model.sav was originally trained with scikit-learn 0.24.2, so loading
# it under a newer scikit-learn prints an InconsistentVersionWarning. The model
# still loads and predicts, but for guaranteed-identical results retrain the
# model (see backend/thyroid/Model Training.ipynb) or pin scikit-learn==0.24.2.
Flask==3.1.3
numpy==2.5.1
scikit-learn==1.9.0
